@page "/person"

<View TEntity=Person Title="Person">
    <Fields>
        <TextField TModel=Person For="(p => p.Id)" Columns=4 />
        <TextField TModel=Person For="(p => p.Name)" Columns=8 />
    </Fields>
    <Columns>
        <Column TModel=Person Value="(p => p.Id)" />
        <Column TModel=Person Value="(p => p.Name)" />
    </Columns>
</View>

@code {
    //public Person Person { get; set; } = new() { Name = "a", Email = "a@a.com", BirthDate = DateTime.Now };
}
